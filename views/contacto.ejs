<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Contáctanos | Grupo Activa</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body { background: #f8f9fa; font-family: 'Roboto', sans-serif; }
    .contact-container {
      max-width: 540px;
      margin: 2.8rem auto 2rem auto;
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 4px 18px rgba(13,55,241,0.06);
      padding: 2.4rem 2rem 1.7rem 2rem;
      position: relative;
    }
    .contact-title {
      text-align: center;
      margin-bottom: 2rem;
      font-family: 'Montserrat', sans-serif;
      font-size: 2.2rem;
      font-weight: 700;
      background: linear-gradient(90deg, #0d37f1 0%, #2deede 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .form-label {
      color: #0d37f1;
      font-weight: 500;
      margin-bottom: .3rem;
    }
    .form-control, .form-select {
      border-radius: 10px;
      font-size: 1rem;
      box-shadow: none;
      border: 1.5px solid #e0eaff;
    }
    .form-control:focus, .form-select:focus {
      border-color: #2deede;
      box-shadow: 0 0 4px #0d37f180;
    }
    .btn-contacto-grad {
      background: linear-gradient(90deg, #0d37f1 0%, #2deede 100%);
      color: #fff;
      border: none;
      border-radius: 30px;
      font-weight: 500;
      font-size: 1.07rem;
      padding: .55em 1.7em;
      display: flex;
      align-items: center;
      gap: .6em;
      justify-content: center;
      box-shadow: 0 1px 6px rgba(13,55,241,0.06);
      transition: background .18s, filter .15s;
      min-width: 140px;
      width: auto;
      margin: 0 auto;
    }
    .btn-contacto-grad:hover {
      filter: brightness(1.1);
      color: #fff;
      background: linear-gradient(90deg, #2deede 0%, #0d37f1 100%);
      text-decoration: none;
    }
    .required { color: #e03131; }
  </style>
</head>
<body>

  <%- include('partials/header', { pageBanner: false }) %>

  <main>
    <div class="contact-container">
      <div class="contact-title">
        <i class="bi bi-envelope-paper-heart"></i> Contáctanos
      </div>
      <form action="/contacto" method="post" autocomplete="off">
        <div class="mb-3">
          <label for="nombre" class="form-label">Nombre completo <span class="required">*</span></label>
          <input type="text" id="nombre" name="nombre" class="form-control" required maxlength="120" placeholder="Tu nombre completo">
        </div>
        <div class="mb-3">
          <label for="correo" class="form-label">Correo electrónico </label>
          <input type="email" id="correo" name="correo" class="form-control" required maxlength="120" placeholder="Opcional">
        </div>
        <div class="mb-3">
          <label for="telefono" class="form-label">Teléfono / Celular <span class="required">*</span></label>
          <input type="tel" id="telefono" name="telefono" class="form-control" required maxlength="12" pattern="[0-9]+" placeholder="Ej: 987654321">
        </div>
        <div class="mb-3">
          <label for="dni" class="form-label">DNI <span class="required">*</span></label>
          <input type="text" id="dni" name="dni" class="form-control" maxlength="15" placeholder="Número de DNI">
        </div>
        <div class="mb-3">
          <label for="monto_solicitado" class="form-label">Monto solicitado <span class="required">*</span></label>
          <input type="number" id="monto_solicitado" name="monto_solicitado" class="form-control" min="100" max="50000" required placeholder="Ej: 1500">
        </div>
        <div class="mb-3">
          <label for="mensaje" class="form-label">Mensaje / Consulta</label>
          <textarea id="mensaje" name="mensaje" class="form-control" rows="3" maxlength="500" placeholder="Cuéntanos tu necesidad"></textarea>
        </div>
        <div class="text-center mt-4">
          <button type="submit" class="btn btn-contacto-grad">
            <i class="bi bi-send"></i> Enviar mensaje
          </button>
        </div>
      </form>
    </div>
  </main>

  <%- include('partials/footer') %>

</body>
</html>
