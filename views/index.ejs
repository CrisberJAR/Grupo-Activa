<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Inicio | Grupo Activa</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    body {
      font-family: system-ui, sans-serif;
      background-color: white;
      color: #333;
      margin: 0;
    }
    /* GRID LAYOUT */
    .content-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      grid-template-rows: auto 1fr;
      gap: 2rem;
      align-items: center;
      padding: 60px 20px;
    }
    /* LOGO + BADGE */
    .logo-display {
      grid-column: 1 / 2;
      grid-row: 1 / 2;
      width: 100%;
      height: 200px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .logo-display-img {
      width: 100%;
      max-width: 200px;
      height: auto;
      object-fit: contain;
    }
    .badge-gradient {
      display: inline-block;
      margin-top: 1rem;
      padding: 8px 20px;
      border-radius: 30px;
      font-weight: bold;
      font-size: 1rem;
      text-transform: uppercase;
      background: linear-gradient(to right, #0d37f1, #2deede);
      color: white;
      letter-spacing: 1px;
    }
    /* HERO CONTENT */
    .hero-content-grid {
      grid-column: 1 / 2;
      grid-row: 2 / 3;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
    .lead-small {
      font-size: 1.25rem;
      font-weight: 500;
      color: #555;
      margin: 1rem 0 2rem;
    }
    .main-title {
      font-size: 3.8rem;
      font-weight: bold;
      background: linear-gradient(to right, #0d37f1, #2deede);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 2rem;
    }
    .btn-primary {
      background-color: #0069d9;
      border: none;
      padding: 10px 20px;
      font-weight: bold;
    }
    .btn-outline-primary {
      border: 2px solid #0069d9;
      color: #0069d9;
      padding: 10px 20px;
      font-weight: bold;
    }
    /* PROFILE IMAGE CARD */
    .profile-grid {
      grid-column: 2 / 3;
      grid-row: 1 / 3;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .profile {
      position: relative;
      height: 75vw;
      width: 75vw;
      max-width: 40rem;
      max-height: 40rem;
      border-radius: 5vw;
      background: linear-gradient(120deg, #0d37f1 0%, #2deede 100%);
    }
    .profile-img {
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      height: 80vw;
      max-height: 45rem;
      border-radius: 5vw;
    }
  </style>
</head>
<body>
  <!-- NAVBAR -->
  <%- include('partials/header', {
        pageBanner:   false
      }) %>

  <!-- GRID: Logo+Badge / Hero / Profile -->
  <section class="content-grid">
    <!-- LOGO + BADGE -->
    <div class="logo-display">
      <img
        src="/assets/logo.png"
        alt="Logo Grupo Activa"
        class="logo-display-img"
      />
      <span class="badge-gradient">CRÉDITOS · AHORRO · ASESORÍA</span>
    </div>

    <!-- HERO CONTENT -->
    <div class="hero-content-grid">
      <p class="lead-small">
        Te ayudamos a crecer con soluciones financieras personalizadas.
      </p>
      <h1 class="main-title">
        Impulsa tu futuro<br />
        con Grupo Activa
      </h1>
      <div class="d-flex flex-wrap gap-4">
        <a href="/simulador" class="btn btn-primary">Necesito un Prestamo</a>
        <a href="/servicios" class="btn btn-outline-primary">Ver planes</a>
      </div>
    </div>

    <!-- PROFILE IMAGE CARD -->
    <div class="profile-grid">
      <div class="profile">
        <img
          class="profile-img"
          src="/assets/profile.png"
          alt="Equipo Grupo Activa"
        />
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <%- include('partials/footer') %>
</body>
</html>
