<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Grupo Activa</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: system-ui, sans-serif;
      background-color: white;
      color: #333;
      margin: 0;
    }

    .navbar {
      background-color: white;
    }

    .navbar-brand {
      font-weight: bold;
      color: #1e30f3;
    }

    .navbar a {
      color: #333;
      font-weight: 500;
    }

    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column-reverse;
      gap: 3rem;
      padding: 60px 20px;
    }

    @media (min-width: 992px) {
      .hero {
        flex-direction: row;
      }
    }

    .hero-content {
      max-width: 600px;
      text-align: center;
    }

    @media (min-width: 992px) {
      .hero-content {
        text-align: left;
      }
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: bold;
      background: linear-gradient(to right, #0d37f1, #2deede);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-top: 1.5rem;
    }

    .hero p {
      font-size: 1.2rem;
      margin: 1rem 0 2rem 0;
    }

    .btn-primary {
      background-color: #0069d9;
      border: none;
      padding: 10px 20px;
      font-weight: bold;
    }

    .btn-outline-primary {
      border: 2px solid #0069d9;
      color: #0069d9;
      padding: 10px 20px;
      font-weight: bold;
    }

    .profile {
      position: relative;
      height: 75vw;
      width: 75vw;
      border-radius: 5vw;
      margin-top: 5vw;
      max-height: 40rem;
      max-width: 40rem;
      background: linear-gradient(120deg, #0d37f1 0%, #2deede 100%);
    }

    .profile-img {
      height: 80vw;
      max-height: 45rem;
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      border-radius: 5vw;
    }

    .badge-gradient {
      display: inline-block;
      padding: 8px 20px;
      border-radius: 30px;
      font-weight: bold;
      font-size: 1rem;
      text-transform: uppercase;
      background: linear-gradient(to right, #0d37f1, #2deede);
      color: white;
      letter-spacing: 1px;
    }

    .main-title {
      font-size: 3.8rem;
      font-weight: bold;
      background: linear-gradient(to right, #0d37f1, #2deede);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .lead-small {
      font-size: 1.25rem;
      font-weight: 500;
      color: #555;
    }

    footer {
      background: #fff;
      color: #444;
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav class="navbar navbar-expand-lg shadow-sm px-4">
    <a class="navbar-brand" href="#">Grupo Activa</a>
    <div class="ms-auto d-flex gap-3">
      <a class="nav-link" href="#">Inicio</a>
      <a class="nav-link" href="/servicios">Servicios</a>
      <a class="nav-link" href="#">Simulador</a>
      <a class="nav-link" href="#">Contacto</a>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-content">
      <span class="badge-gradient">CRÉDITOS · AHORRO · ASESORÍA</span>
      <p class="lead-small mt-3 mb-4">Te ayudamos a crecer con soluciones financieras personalizadassssss.</p>
      <h1 class="main-title display-title">Impulsa tu futuro<br>con Grupo Activa</h1>
      <div class="d-flex flex-wrap gap-4 mt-4 justify-content-center justify-content-lg-start">
      <a href="#" class="btn btn-primary px-4 py-2">Simula tu crédito</a>
      <a href="#" class="btn btn-outline-primary px-4 py-2">Ver planes</a>
    </div>

    </div>
    <div class="profile">
      <img class="profile-img" src="/assets/profile.png" alt="Grupo Activa" />
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    &copy; 2025 Grupo Activa – Todos los derechos reservados
  </footer>

</body>
</html>